// Your JS code is here
function Num(first, second) {
  if (first > second) {
    return first
  } else if (first < second) {
    return second
  }
  return 'Error: they are equal!'
};
Num(20, 19.99);
