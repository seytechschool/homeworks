// Your JS code is here
function pluralize (num, noun) {
    
    
    var pluralForm= "There are " + num + noun + "s in the room."
    return pluralForm
  
}
console.log (pluralize (20, ' student'))
console.log (pluralize (4, ' professor'))
console.log (pluralize (4, ' chair'))
