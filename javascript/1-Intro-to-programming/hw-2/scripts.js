// Your JS code is here

// Version 1
var currentYear = 2020;
var birthYear = 1990;
var age = currentYear - birthYear;
console.log(`They are either ${age} or ${age - 1}`)

// Vestion 2. Function
const ageCalculator = (currentYear, birthYear) => `They are either ${currentyear-birthyear} or ${currentyear-birthyear - 1}`;